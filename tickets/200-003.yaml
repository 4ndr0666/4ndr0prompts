id: 200-003
stream: QA
title: Restore Bats Test Suite (tests/cli.bats)
dependencies: []
priority: P0
est_hours: 1
description: |
  Automate interactive flow testing with a dummy fzf and wl-copy.
acceptance_criteria:
  - Simulates user input for each slot via dummy fzf.
  - Verifies clipboard functionality via wl-copy simulation.
  - All tests pass via make test.
deliverables:
  - tests/cli.bats
notes: |
  Uses PATH to override fzf and wl-copy during tests.
