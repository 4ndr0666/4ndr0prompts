id: 200-009
stream: CI
title: Verify GitHub Actions Workflows
dependencies: []
priority: P0
est_hours: 1
description: |
  Confirm and simplify CI pipelines.
acceptance_criteria:
  - CI runs ruff, shellcheck, Python and shell tests.
  - Adjust release workflow if not needed.
deliverables:
  - .github/workflows/ci.yml
notes: |
  Ensure docs-only changes skip checks.
